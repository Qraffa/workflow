# SDD + TDD 工程实践融合最终方案

## 1. 方案定位

本方案用于长期维护、多人协作、业务行为需要稳定交付的工程项目。它不是把 SDD 和 TDD 简单叠加，也不是让 Spec 替代测试，或让测试反向定义需求，而是在不同节奏、不同抽象层级上分配职责：

- SDD 负责定义业务意图、外部契约、关键约束和交付边界。
- TDD 负责在行为切片内小步实现、暴露设计反馈、提供回归保护。
- Scenario 是 SDD 与 TDD 的桥接单位。
- Test 是 Spec 的可执行证据，但不是外部业务契约的默认事实来源。
- Code 是实现结果，必须同时满足 Spec 表达的意图和 Test 提供的证据。

最终目标是让 Spec、Test、Code 保持行为语义一致，而不是追求文档条目、测试用例、代码结构的逐条机械同步。

## 2. 核心思想

### 2.1 SDD 的核心思想

SDD 的价值在于先把“为什么做、做什么、做到什么边界”说清楚，再进入实现。它关注的是系统对外承诺，而不是提前设计所有内部结构。

SDD 应定义：

- 目标：本次 change 要解决的问题和业务价值。
- 范围：明确包含哪些能力、流程、接口或规则。
- 非目标：明确不做什么，防止隐性扩张。
- Scenario：可验证的外部行为。
- 外部契约：API、数据模型、事件、错误语义、权限、安全、兼容性等。
- 关键约束：性能阈值、审计要求、可观测性要求、迁移约束等。
- 任务切片：能独立实现、验证和交付的行为增量。

SDD 不应定义：

- 私有类、私有函数、内部目录结构。
- 具体缓存、数据库、队列、框架选择，除非它本身就是外部约束。
- 过细的单元测试清单。
- 尚未稳定的探索性判断。

### 2.2 TDD 的核心思想

TDD 的价值在于用快速反馈驱动实现，并让重构始终处在测试保护下。它关注的是“当前行为如何被证明正确”，而不是一次性写完所有测试。

标准循环是：

```text
Red: 写一个针对当前行为的失败测试
Green: 写最少代码让测试通过
Refactor: 在全绿保护下改善结构、命名、抽象和重复
```

TDD 的关键约束：

- 一次只推进一个行为或一个规则。
- 测试应验证可观察行为，优先通过公共接口验证。
- 不批量预写大量测试后再批量实现，避免水平切片。
- 不在 Red 状态下重构。
- 不为了未来假设添加 speculative code。
- 单测可以服务内部设计反馈，但不应把内部实现细节伪装成业务契约。

### 2.3 融合后的基本判断

SDD 和 TDD 的张力主要来自“设计发现权”：

- SDD 希望在实现前稳定外部契约。
- TDD 希望在实现中通过反馈发现更自然的设计。

最终取舍是：

- 外部业务语义、稳定接口、数据契约和关键约束，设计发现权前移到 SDD。
- 内部结构、模块协作、命名、抽象和可测试性，设计发现权保留给 TDD。
- 如果 TDD 发现外部契约需要改变，通过 Escape Hatch 回到 Spec，而不是让测试悄悄改写契约。

## 3. 分层职责与事实来源

| 层级  | 节奏              | 主导方法      | 主要产物                                       | 事实来源         |
| --- | --------------- | --------- | ------------------------------------------ | ------------ |
| 契约层 | 一次 change，天/周   | SDD       | Proposal、Design、Spec、Scenario、Tasks        | Spec         |
| 验证层 | 一个行为切片，小时/天     | SDD + TDD | Acceptance、Integration、Contract、Unit Tests | Test 作为可执行证据 |
| 实现层 | 一个 TDD 循环，分钟/小时 | TDD       | 测试、实现代码、重构结果                               | Test / Code  |

职责裁决规则：

| 内容类型                     | 主要事实来源      | 处理原则                                     |
| ------------------------ | ----------- | ---------------------------------------- |
| 业务目标、用户流程、验收标准           | Spec        | 必须在实现前澄清到可验证程度                           |
| 外部 API、事件、数据模型、权限、安全、兼容性 | Spec        | 变更必须回到 Spec                              |
| 错误码、错误语义、边界条件、性能阈值       | Spec 优先     | 只要对外可见或业务相关，就进入 Spec；纯内部细节留在 Test / Code |
| 验收路径、契约行为、关键业务规则证据       | Test        | 用自动化测试或明确验证证据证明                          |
| 内部结构、命名、抽象、私有实现          | Test / Code | 由 TDD 和重构反馈主导                            |

冲突处理：

- Spec 与 Test 不一致时，先澄清业务意图。
- 如果 Spec 错，先改 Spec，再同步 Test 和 Code。
- 如果 Test 错，改 Test，不反向污染 Spec。
- 如果 Code 错，改 Code，并用测试证明修复。
- 如果冲突涉及外部可观察行为，默认暂停实现并进入 Escape Hatch。

## 4. 标准工作流程

```text
Explore / Clarify
        ↓
SDD 定义大契约
        ↓
Scenario 描述可验证行为
        ↓
拆分行为切片和任务
        ↓
为切片派生测试策略
        ↓
每个切片内执行 TDD:
Red → Green → Refactor
        ↓
分层处理反馈
        ↓
Verify: 校验 Spec / Test / Code 语义一致
        ↓
Archive / Merge
```

流程要点：

- Explore 阶段先澄清业务语言、约束和不做什么。
- Spec 阶段只稳定外部契约，不提前锁死内部实现。
- Tasks 应按行为切片拆分，而不是按技术层横向拆分。
- TDD 在每个切片内部执行，不把 Red 理解为“先写完全部测试”。
- Verify 检查语义一致性，不做形式主义勾选。

## 5. Scenario 作为桥接点

Scenario 是 Spec 与 Test 之间最重要的桥接单位。它应描述一个外部可观察、可验证、可独立讨论的行为。

推荐格式：

```text
Scenario ID: auth.login.001
Given: 用户存在且密码正确
When: 用户提交登录请求
Then: 系统签发会话并返回成功响应
```

合格 Scenario 标准：

- 一个 Scenario 只描述一个独立可验证的行为。
- Given / When / Then 使用项目业务语言。
- When 能映射到一次明确调用、事件或用户动作。
- Then 是外部可观察结果，如返回值、状态变化、事件、副作用、错误响应。
- 不混入多个行为。
- 不写“用户体验更好”这类不可验证目标。
- 不写 Redis、私有类、内部算法等实现细节，除非它们本身是外部约束。

追溯原则：

- 重要 active Scenario 应有自动化测试或明确验证证据。
- 外部契约和关键业务行为的测试建议引用 Scenario ID。
- 普通 unit test 不强制绑定 Scenario ID。
- Test 可以暴露 Spec 缺失，但不能单独定义新的外部契约。

示例测试标记：

```text
@spec("auth.login.001")
```

## 6. 行为切片与任务拆分

行为切片是从 Spec 进入 TDD 的最小交付单位。每个切片应满足：

- 对应一个明确的行为增量。
- 可以独立实现、测试和验收。
- 有清楚的输入、触发条件和外部可观察结果。
- 能说明主要验证方式。
- 尽量能在少量 TDD 循环内完成。

切片过大时，TDD 反馈会变慢；切片过小时，任务会退化成技术步骤，看不出业务行为增量。

推荐优先级排序：

1. 高业务价值路径。
2. 高风险或高失败成本路径。
3. 外部接口或数据契约。
4. 易回归路径。
5. 实现依赖的基础行为。

## 7. 从 Spec 派生测试策略

测试策略按行为、风险和契约层级决定，不按“每条 Spec 对应多少测试”机械分配。

| Spec 内容 | 推荐测试层级 |
|---|---|
| 主要验收路径 | Acceptance / Integration Test |
| 跨模块业务流程 | Integration Test |
| 外部 API、SDK、消息格式、事件契约 | Contract Test |
| 核心业务规则、状态机、金额、权限判断 | Unit Test + Integration Test |
| 错误处理和边界条件 | Unit Test 或 Integration Test，按影响范围选择 |
| 安全、性能、兼容性约束 | 专项测试、自动化检查或审计证据 |
| 内部辅助逻辑 | Unit Test，必要时覆盖 |

测试原则：

- 验收测试证明用户路径成立。
- 集成测试证明模块协作成立。
- 契约测试证明外部接口稳定。
- 单测证明关键规则、边界和内部设计压力点。
- 测试应尽量通过公共接口验证行为。
- Mock 应用于隔离外部系统或昂贵依赖，避免 mock 内部协作者导致测试耦合实现。

## 8. 切片内 TDD 执行规则

每个行为切片内执行小循环：

```text
1. 选择当前切片中的一个行为或规则
2. 写一个因正确原因失败的测试
3. 写最少代码让它通过
4. 跑相关测试
5. 在全绿状态下重构
6. 重复直到该切片完成
7. 在切片边界跑更高层级或更完整的测试
```

每个循环检查：

- 测试是否描述行为，而不是实现步骤。
- 测试是否通过公共接口或稳定契约验证。
- 当前代码是否只是为了通过当前测试。
- 是否没有提前实现未来能力。
- 重构是否发生在全绿状态下。
- 本轮是否发现了需要回到 Spec 的外部行为变化。

本地验证策略：

- 每个小循环跑相关快速测试。
- 每个切片完成时跑相关单测、集成测试或契约测试。
- 合并前由 CI 跑完整测试套件。
- 对高风险模块，可要求本地或预合并阶段跑完整测试。

## 9. Refactor 边界规则

| 改动范围 | 处理方式 |
|---|---|
| 内部结构、命名、私有函数、局部抽象 | 在 TDD 内自由重构 |
| 测试表达、fixture、mock 策略 | 在 TDD 内调整 |
| 仅团队内部消费的模块接口 | 评估影响范围，必要时更新 Design 或 Tasks |
| 外部消费者可见的稳定接口 | 必须回到 Spec 或走 Escape Hatch |
| 可观察行为、错误语义、数据模型、权限、安全、性能承诺 | 必须回到 Spec 或走 Escape Hatch |

判断准则：

- 只影响代码内部可维护性，留在 TDD。
- 影响模块协作但不影响外部契约，更新设计说明或任务。
- 影响业务语义、验收标准或外部消费者，回到 Spec。

## 10. Escape Hatch

Escape Hatch 是从 TDD 小循环临时回到 SDD 的轻量通道。它用于处理“继续写代码会把错误语义固化”的情况。

必须触发的场景：

- TDD 中发现 Scenario 语义错误、歧义或矛盾。
- 测试发现 Spec 未覆盖的外部可观察边界。
- 重构中浮现出更合适的外部接口或契约。
- Bug 修复会改变外部行为或验收标准。
- 实现成本明显超出预期，需要重新切片、缩小范围或改设计。

推荐标签：

| 标签 | 含义 |
|---|---|
| `[escape:spec-error]` | Spec 语义错误、歧义或矛盾 |
| `[escape:scenario-missing]` | 缺失外部可见边界或业务规则 |
| `[escape:better-interface]` | 需要调整外部接口或契约 |
| `[escape:scope-overflow]` | 实现成本或范围明显超出预期 |

处理流程：

```text
1. 暂停当前 TDD 小循环。
2. 显式标记当前状态，例如 pending test、WIP commit 或任务备注。
3. 做 mini-spec-update，只修改相关 Scenario、验收标准、设计说明或任务。
4. 更新测试策略和必要测试。
5. 恢复 TDD。
6. Verify 阶段确认该 Escape 已闭环。
```

治理规则：

- 单个 change 中 Escape 超过 3 次，应重新评审 Spec 或任务切片。
- 单个 change 中超过一半任务触发 Escape，应退出标准 SDD 流程，转入探索或需求澄清。
- 团队持续高频 Escape，说明 Spec 质量、业务理解或切片方式存在系统性问题。

这些阈值在 L0 可作为 Review 提醒，在 L2 以后再做自动统计和门禁。

## 11. 变更类型处理

| 变更类型 | SDD | TDD | 处理方式 |
|---|---|---|---|
| 新功能或重要业务行为变化 | 完整执行 | 完整执行 | 先定义或更新 Spec，再按切片 TDD |
| 外部 API 或数据契约变化 | 完整执行 | 完整执行 | Spec + Contract Test + TDD |
| Bug 修复 | 视情况 | 完整执行 | 判断是否暴露 Spec 缺陷或改变外部行为 |
| 内部重构 | 通常不需要 | Refactor only | 不改变外部契约，不改 Spec |
| 性能优化 | 视情况 | 完整执行 | 若有外部性能承诺，写入 Spec |
| 紧急热修复 | 可暂跳 | 必须补测试 | 先止血，事后限期补 Spec 和测试 |
| 原型或探索 | 轻量或跳过 | 轻量 | 不作为稳定契约直接合入主干 |

Bug 修复升级判断：

- 现有 Scenario 是否定义了相关行为？如果没有，补 Spec。
- 修复是否改变可观察行为？如果改变，补 Spec。
- 是否只是实现不符合现有 Spec？如果是，直接用 TDD 修复。
- 是否只是内部实现错误且外部契约不变？可以只改测试和代码。

紧急热修复规则：

- P0 可先跳过完整 SDD，但必须补必要测试。
- 热修复后应在约定时间内补齐 Spec、Scenario 或任务记录。
- 若逾期未补，应阻塞后续相关发版或至少进入发布风险清单。

## 12. Verify 一致性校验

Verify 的目标是证明 Spec / Test / Code 在行为语义上保持一致。

合并前检查：

| 编号 | 检查内容 |
|---|---|
| V1 | Spec 是否仍表达真实业务意图和当前范围 |
| V2 | 主要 active Scenario 是否有自动化测试或明确验证证据 |
| V3 | 外部接口、错误语义、数据模型、权限、安全、性能承诺是否与 Spec 一致 |
| V4 | Acceptance / Integration / Contract Tests 是否覆盖主要验收路径和外部契约 |
| V5 | Unit Tests 是否覆盖关键规则、边界和复杂状态 |
| V6 | 本次 Escape Hatch 是否全部闭环 |
| V7 | TDD 中发现的新业务语义是否已回填 Spec |
| V8 | 是否没有把内部实现细节误写进 Spec |

Verify 不要求：

- 每条 unit test 都绑定 Scenario。
- 每条 Scenario 都有唯一测试。
- 文档结构与代码结构一一对应。
- 每次内部重构都更新 Spec。

Verify 必须要求：

- 外部行为无漂移。
- 关键约束无遗漏。
- 测试证据可信。
- 代码满足当前 Spec 和测试。

## 13. 工具链与门禁分级

工具链应随流程成熟度和项目风险逐步增强，不应从重型治理开始。

### L0：人工可启动

适用：首次试点、小团队、流程尚未稳定。

要求：

- 有 Spec、Scenario、Tasks。
- 切片内执行 TDD。
- PR 模板包含 Verify 清单。
- Code Review 检查 Scenario 质量、测试证据和外部契约。

不要求：

- 自动追溯。
- 仪表盘。
- hash 门禁。
- Red-First 自动检测。

升级标准：

- 至少一个 pilot change 完整走通。
- 团队能写出粒度合理的 Scenario。
- 上线后没有明显 Spec / Test / Code 漂移。

### L1：轻量治理

适用：团队开始稳定使用。

要求：

- 重要 Scenario 使用稳定 ID。
- 关键验收、集成或契约测试可引用 Scenario ID。
- CI 检查测试通过。
- CI 检查悬空 Scenario 引用。
- CI 对关键外部接口变更给出提示或要求 reviewer 确认。

可选：

- Escape 标签在提交信息或 PR 描述中记录。
- PR 模板要求说明测试证据。

### L2：工程化治理

适用：多人团队、长期系统、高风险模块。

要求：

- 稳定外部接口建立 contract tests。
- Scenario 内容变更触发 reviewer 复核相关测试。
- 统计 Escape Hatch 频率和漂移事件。
- 对高风险模块建立更严格的测试和验收门禁。
- 对外部契约变更要求 Spec ID 与测试证据。

### L3：高治理或合规场景

适用：大团队、强合规、接口长期稳定、AI 协作密集。

可引入：

- Spec / Test 自动追溯图。
- Scenario hash 或版本检查。
- 自动生成治理报表。
- LLM 辅助语义检查。
- 副作用和外部调用审计。

L3 工具只能增强治理，不能替代人对业务语义的判断。

## 14. AI 协作规则

AI 协作时，SDD + TDD 的价值会被放大：Spec 限定生成方向，Test 提供刹车和证据。

推荐机制：

- AI 编码前必须读取相关 Spec、Scenario、Design 和现有测试。
- AI 应先说明将实现哪个行为切片，以及对应验证方式。
- 新增外部接口、错误语义、数据模型或副作用时，必须指出对应 Scenario。
- AI 不应凭空创造未在 Spec 中出现的外部能力。
- AI 可以改进内部结构，但不得改变外部契约，除非触发 Escape Hatch。
- AI 完成一个切片后应运行相关测试，并说明未运行的测试及原因。

防偏离重点：

- 用 Scenario 限定“做什么”。
- 用 TDD 限定“如何证明做对”。
- 用 Verify 检查“是否出现 Spec / Test / Code 漂移”。

## 15. 启动前决策

建议启动 SDD + TDD 的条件：

- 项目预计维护周期超过 3 个月。
- 至少 3 名长期维护者或存在跨角色协作。
- 业务需求有相对稳定边界，不是纯探索。
- 外部接口、数据契约、权限、安全或业务规则具有维护价值。
- 团队愿意维护基本 Spec 质量。
- 至少一名成员能写合格 Scenario 并把关评审。

任意两项不满足时，不建议启动完整 SDD + TDD，应先使用轻量 TDD、探索流程或需求澄清。

## 16. 适用与不适用场景

适用场景：

- 长期维护的产品或平台。
- 多人协作、跨模块变更。
- 业务规则复杂，错误成本高。
- 外部接口或数据契约需要稳定。
- AI-assisted development 中需要约束生成范围。
- 测试、需求和实现容易漂移的系统。

不适用或应降级的场景：

- 一次性脚本、临时工具。
- 早期产品探索，需求每天剧烈变化。
- Demo、POC、短期验证。
- 团队没有维护 Spec 的能力或意愿。
- 需求语言尚未形成共识。

运行中退出信号：

- Escape Hatch 频繁触发，超过任务的一半。
- Spec 改动频率长期高于实现改动频率。
- Spec 评审反复无法收敛。
- 团队把 Spec 当流程负担，而不是意图契约。
- 大量测试与实现细节耦合，重构时频繁误伤。

出现这些信号时，应先退回探索、需求澄清或纯 TDD，而不是继续加重流程。

## 17. 工程取舍

本方案保留：

- SDD 宏观、TDD 微观的分层。
- Scenario 作为桥接点。
- 行为切片和 tracer bullet 式 TDD。
- Refactor 边界规则。
- Escape Hatch。
- Verify 一致性校验。
- 渐进治理路径。
- 不适用场景和退出信号。

本方案降级或移除：

- 不强制每个 unit test 绑定 Spec。
- 不要求每个 TDD 小循环更新 Spec。
- 不把 Spec 写成测试清单。
- 不把所有 public/exported 函数都纳入 Spec，只关注外部消费者可见的稳定接口。
- 不用脆弱的 pre-commit hook 机械判断 Red-First。
- 不在起步阶段引入仪表盘、周报统计、全量 hash 门禁、LLM 语义检查。
- 不采用绝对的“Spec 永远优先”，而是按职责边界裁决冲突。

最终原则：

```text
SDD 定义外部契约和业务意图。
Scenario 将意图拆成可验证行为。
Task 将行为落到可交付切片。
TDD 在切片内驱动实现和内部设计。
Escape Hatch 处理越界反馈。
Verify 保证 Spec / Test / Code 语义一致。
```

这套流程成立的关键不是文档数量，也不是测试数量，而是三件事：

- Spec 是否说清真实业务意图。
- Test 是否提供足够可信的可执行证据。
- Code 是否在可维护的结构下实现了这些行为。
