# Slices for <change-id>

> Generated by slicespec-slice on YYYY-MM-DD.
> Source of truth for which slices exist, their boundaries, and their
> progress. state.json projects from this file.

## Summary

| Slice | Title | Type | Covers | Cycles | Status |
|---|---|---|---|---|---|
| s01 | <title> | AFK | auth.login.001 | 3 | pending |
| s02 | <title> | HITL | auth.login.002 | 5 | pending |
| ... | ... | ... | ... | ... | ... |

## Dependency Graph

```
s01 ──┬──> s03 ──> s05
      └──> s04
s02 ─────────────────> s05
```

(Use ASCII art. Edges go from blocker to blocked. A node with no incoming
edges is initially unblocked.)

## Parallel guards (project-wide)

These paths cannot be touched by parallel slices. The controller in
slicespec-implement consults this list before dispatching.

- `**/migrations/**`
- `**/config/**`
- `**/*.proto`
- `**/*.graphql`
- ... (append project-specific high-conflict paths)

---

## Slice s01: <title>

- **type**: AFK
- **covers**: auth.login.001, auth.login.002
- **blocked_by**: none
- **write_scope**:
  - `src/auth/**`
  - `tests/auth/**`
- **do_not_touch**:
  - `specs/**`
  - `changes/**` (except this slice's own evidence/ directory)
  - `.github/**`
  - `CONTEXT.md`
  - `docs/adr/**`
  - `src/billing/**`
  - lock files
- **requires_shared_resource**: none
- **test_strategy**:
  - acceptance: end-to-end happy-path login covering `auth.login.001`
  - unit: password-failure counter logic for `auth.login.002`
- **estimated_cycles**: 3
- **status**: pending
  <!-- Status transitions are managed by slicespec-implement and
       slicespec-escape. Status values: pending, in_progress, done,
       blocked, escaped. Details (phase, owner) live in state.json. -->

### Notes

<Optional. Anything the implementer should know that does not fit the
fields above. Examples: external library quirks, known fragile tests,
non-obvious failure modes.>

---

## Slice s02: <title>

- **type**: HITL
- **covers**: auth.login.003
- **blocked_by**: s01
- **write_scope**:
  - `src/auth/lockout.py`
  - `tests/auth/test_lockout.py`
- **do_not_touch**:
  - <defaults>
- **requires_shared_resource**: none
- **test_strategy**:
  - integration: lockout flow with persistent counter
- **estimated_cycles**: 5
- **status**: pending

### Notes

<HITL slices should record the user-judgement steps explicitly. Example:
"User to decide lockout duration before implementer starts.">

---

<!--
slicespec-slice exit checklist:
- [ ] Each slice has all required fields filled
- [ ] No slice has estimated_cycles ≥ 10 without explicit user override + reason
- [ ] No slice has empty test_strategy
- [ ] Dep graph rendered and consistent with blocked_by lists
- [ ] write_scope and do_not_touch listed for every slice
- [ ] Parallel guards listed at top
- [ ] state.json updated: status="implementing", slices{} populated
-->
